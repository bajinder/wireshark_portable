# Appfile
#
# Thin, app-specific copy of the canonical fastlane Appfile template.
# Canonical source of truth: ~/infra/templates/fastlane/Appfile
# Keep this file in sync with the template — only `app_identifier` and
# `team_id`/`apple_id` overrides should differ per app.

app_identifier("com.bajinder.catchbook")
apple_id(ENV["FASTLANE_APPLE_ID"] || "")
team_id(ENV["FASTLANE_TEAM_ID"] || "")
